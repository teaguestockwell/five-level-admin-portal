If there are no aircraft the admin portal will throw

Search bar filter List<Map<String,dynamic>> into another [] based on Map[] based on Map[name || title || email]
  Should I rename all these filter keys to name in prisma?

Title above table

How can i diffrentate between the ep while still keepping tables dynmaic 

{
  New Buttons

  Edit screen

  Config cargos modal. 
}